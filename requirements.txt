# Direct dependency snapshot verified in the publication-artifact environment.
# Install the local package separately with: python -m pip install -e . --no-deps

# Numerical and reporting stack
matplotlib==3.10.9
numpy==2.4.4
pandas==3.0.3
PYPOWER==5.1.19
PyYAML==6.0.3
scipy==1.17.1

# Test and lint tooling
pytest==9.0.3
ruff==0.15.12

# QSVT, circuit, and phase-synthesis paths
mpmath==1.3.0
pennylane==0.45.0
pyqsp==0.2.0
qiskit==2.4.1
qiskit-aer==0.17.2
sympy==1.14.0
